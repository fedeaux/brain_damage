- contact ||= @contact

%tbody.brain-damage-list-item-wrapper
  %tr.brain-damage-list-item{ :data => { :item => { :id => contact.id }}}
    %td= contact.name
    %td= contact.title
    %td= contact.contact_role_id
    %td= contact.ac_info
